program: celebA_noise_128.py
method: grid
parameters:
  dataset:
    values:  [celebA]
  optim:
    values: ['Adam']
  loss_type:
    values: ['l2']
  batch_size:
    values: [32]
  lr:
    values: [2e-5]
command:
  - ${env}
  - python
  - ${program}
  - ${args_no_boolean_flags}